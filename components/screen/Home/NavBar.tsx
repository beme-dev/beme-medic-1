import { FC } from 'react';
import { Text, View } from 'react-native';

type Props = {};

export const TabNav: FC<Props> = ({}) => {
  return (
    <View>
      {/* TODO: List the nav icons */}
      <Text>Hello World</Text>
    </View>
  );
};
